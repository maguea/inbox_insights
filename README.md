# Inbox Insight
## Getting started
you may need the python extension

run the project with `python -m src.app`
### web app
make sure you have the venv set up. `pip install flask` and run app.py. you should see the IP:PORT in the terminal. you can ctrl+click on it and it should take you to the browser